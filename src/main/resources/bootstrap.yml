spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://fcdc.vip:8848 #Nacos服务注册中心地址
      config:
        server-addr: http://fcdc.vip:8848
        file-extension: yaml #指定yaml格式的配置

management:
  endpoint:
    health:
      show-details: always


nacos:
  config:
    server-addr:  http://fcdc.vip:8848
